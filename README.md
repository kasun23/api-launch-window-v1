# api-launch-window-v1
-API Calls
* All locations
http://{hostname}:8081/launch/locations
Sorted based on all locations launch scores

* Named locations
http://{hostname}:8081/launch/locations?name={location name}
Loacation specific sorting

